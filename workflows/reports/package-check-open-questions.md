# Package-check open questions

## Complete source-level suite could not create a preprocessCore thread

Status: resolved by maintainer verification

Raised: 2026-07-31

Resolved: 2026-07-31

The package-check workflow validated its artifact and local environment, and
the documentation-source child workflow completed successfully. The final
standalone documentation-source review also passed for all eight affected
Roxygen-bearing sources, and both declared vignettes built successfully in
separate clean R processes. The test workflow validated its artifact,
environment, and reviewed plan. Its focused run passed eleven testthat
contexts covering edits in these nine R sources:

- `R/colors_for_annotation.R`
- `R/correct_batch_effects.R`
- `R/initial_assessment.R`
- `R/metadata_diagnostics.R`
- `R/plot_NA_intensity.R`
- `R/plot_helpers.R`
- `R/proBatch.R`
- `R/proteome_wide_diagnostics.R`
- `R/utility_funcs.R`

The eleven passing contexts were `test-batch_effect_steps.R`,
`test-colors_for_annotation.R`, `test-correct_batch_effect.R`,
`test-explained_variance_plots.R`, `test-initial_assessment.R`,
`test-metadata_diagnostics.R`, `test-plot_NA_intensity.R`,
`test-plot_helpers.R`, `test-proBatch.R`,
`test-proteome_wide_diagnostics.R`, and `test-utility_funcs.R`.

The required complete source-level suite passed every context except two calls
in `tests/testthat/test-normalize.R`:

```text
test-normalize.R:5:5
Error in normalize.quantiles(data_matrix):
ERROR; return code from pthread_create() is 22

test-normalize.R:110:5
Error in normalize.quantiles(data_matrix):
ERROR; return code from pthread_create() is 22
```

Both errors originate in the unchanged
`preprocessCore::normalize.quantiles()` call from `R/normalize.R`. Neither that
source nor `tests/testthat/test-normalize.R` was inside the reviewed repair
boundary. Reproduction with sandbox escalation rules out the cleanup diff and
ordinary sandbox permissions; the precise cause within the existing
Pixi/conda threading runtime remains undiagnosed. The attempts preserved
`pixi.toml`, `pixi.lock`, and the Git-visible non-generated worktree, and
removed the newly generated `tests/testthat/Rplots.pdf`.

The maintainer's subsequent `devtools::check()` completed package tests,
examples, vignettes, S3 consistency, static code analysis, and Rd usage checks
without reproducing either normalization failure. This confirms that the
thread-creation blocker was confined to the agent's Pixi environment and does
not justify a package source or normalization-test change.

The worktree was initially clean, so there were no pre-existing user changes to
preserve. The final cleanup consists of focused changes in nine R sources, five
test files, two vignette sources, `inst/WORDLIST`, and these workflow reports.
No generated manual, generated namespace, dependency installation, Git index,
commit, or published state was touched.

The rerun reduced the package findings to one spelling NOTE generated by the
possessive forms `backend's` and `limma's`. Their Roxygen source prose is now
rephrased in `R/proteome_wide_diagnostics.R` and
`R/correct_batch_effects.R`, respectively, and the Donna documentation-source
review passed. Package-level confirmation remains maintainer-owned: regenerate
the Roxygen documentation and `NAMESPACE`, then rerun the spelling and
package-wide R/Bioconductor checks.

The same environment limitation and prior evidence are retained in
`workflows/reports/open-questions.md`.
