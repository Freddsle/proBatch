% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ProBatchFeatures.R
\name{pb_subset_samples}
\alias{pb_subset_samples}
\title{Subset samples in a `ProBatchFeatures` object using sample metadata.}
\usage{
pb_subset_samples(
  object,
  sample_id_col = "FullRunName",
  subset_by = sample_id_col,
  subset_values
)
}
\arguments{
\item{object}{A `ProBatchFeatures` object.}

\item{sample_id_col}{Character scalar naming the sample identifier column in
`colData(object)`. When missing from `colData(object)`, rownames are used.}

\item{subset_by}{Character scalar naming the metadata column used for
filtering. Defaults to `sample_id_col`.}

\item{subset_values}{Vector of values to keep in `subset_by`.}
}
\value{
A `ProBatchFeatures` object restricted to the selected samples.
}
\description{
This is a thin wrapper around the existing `[` method. It resolves sample
metadata from `colData(object)`, builds a column mask, and delegates the
actual subsetting to the subclass-preserving method above.
}
\examples{
data(example_proteome_matrix, package = "proBatch")
data(example_sample_annotation, package = "proBatch")
sample_annotation <- example_sample_annotation
sample_annotation$Group <- rep(c("Pool", "Study"), length.out = nrow(sample_annotation))
pbf <- ProBatchFeatures(
    data_matrix = example_proteome_matrix,
    sample_annotation = sample_annotation,
    sample_id_col = "FullRunName"
)
pb_subset_samples(
    pbf,
    sample_id_col = "FullRunName",
    subset_by = "Group",
    subset_values = "Pool"
)
}
