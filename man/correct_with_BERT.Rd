% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/correct_batch_BERT_PLSDA.R
\name{correct_with_BERT}
\alias{correct_with_BERT}
\title{BERT-based batch correction (unified; long/matrix)}
\usage{
correct_with_BERT(
  x,
  sample_annotation,
  feature_id_col = "peptide_group_label",
  measure_col = "Intensity",
  sample_id_col = "FullRunName",
  batch_col = "MS_batch",
  format = c("long", "wide"),
  bert_method = c("ComBat", "limma", "ref"),
  combatmode = NULL,
  covariates_cols = NULL,
  keep_all = "default",
  cores = NULL,
  BPPARAM = NULL,
  ...
)
}
\arguments{
\item{x}{Data in long (\code{data.frame}) or wide (\code{matrix}) form, controlled by \code{format}.}

\item{sample_annotation}{data frame with:
\enumerate{ \item \code{sample_id_col} (this can be repeated as row names)
  \item biological covariates
  \item technical covariates (batches etc) }.
  See \code{help("example_sample_annotation")}}

\item{feature_id_col}{name of the column with feature/gene/peptide/protein
ID used in the long format representation \code{df_long}. In the wide
formatted representation \code{data_matrix} this corresponds to the row
names.}

\item{measure_col}{if \code{df_long} is among the parameters, it is the
column with expression/abundance/intensity; otherwise, it is used
internally for consistency.}

\item{sample_id_col}{name of the column in \code{sample_annotation} table,
where the filenames (colnames of the \code{data_matrix} are found).}

\item{batch_col}{column in \code{sample_annotation} that should be used for
batch comparison (or other, non-batch factor to be mapped to color in plots).}

\item{format}{One of \code{"long"} or \code{"wide"}.}

\item{bert_method}{Edge adjustment inside BERT: one of \code{"ComBat"}, \code{"limma"}, \code{"ref"}.
Defaults to \code{"ComBat"}.}

\item{combatmode}{Integer in \code{1:4} passed to BERT (parametric/non-parametric, mean.only).
If \code{NULL}, the wrapper does not pass it and BERT uses its own default.
Ignored if \code{bert_method != "ComBat"}.}

\item{covariates_cols}{Optional character vector of \code{sample_annotation} columns
included in \code{mod} for ComBat (biological or nuisance covariates).}

\item{keep_all}{For long output, columns retained (as in other correct_* functions).}

\item{cores}{Integer; number of workers for BERT. If \code{NULL} and \code{BPPARAM} is also \code{NULL},
the wrapper leaves worker selection to \code{BERT::BERT()}. Provide \code{cores} (and optionally a matching
\code{BPPARAM}) to control parallel execution (see \code{?BiocParallel}).}

\item{BPPARAM}{Optional \code{BiocParallelParam} object for BERT. When \code{NULL}, the wrapper does not
set a default and \code{BERT::BERT()} uses its own default.}

\item{...}{Additional arguments passed to \code{BERT::BERT()}. Note that by default the wrapper sets
\code{referencename = " "} (a single space) unless you override it via \code{...}.}
}
\value{
If \code{format="wide"}, a corrected numeric matrix (features×samples).
  If \code{format="long"}, the original long data with corrected \code{measure_col} and
  a \code{preBatchCorr_[measure_col]} column preserved.
}
\description{
Adjusts batch effects using BERT (Batch-Effect Reduction Trees),
which aligns batches and tolerates NAs.
Works on long data.frames (\code{format="long"}) and wide matrices
(\code{format="wide"}) by converting to/from matrices under the hood.
}
\details{
BERT itself requires a \emph{samples × features} table; proBatch's wrapper builds it from the input
\emph{features × samples} matrix. Missing values (NAs) are allowed.

When invoked through \code{pb_transform()}, the resulting pipeline step is tagged with the chosen
\code{bert_method}: \code{BERTc} (ComBat), \code{BERTl} (limma), or \code{BERTr} (ref). This keeps
method-specific corrections distinct when multiple BERT variants are stored on the same object.
}
\references{
BERT Bioconductor page & manual. Schumann Y. & Schlumbohm S., 2025.
  \doi{10.1038/s41467-025-62237-4}. Nature Communications (2025)
}
\seealso{
\code{\link{correct_batch_effects}}, \code{\link{BERT::BERT}}
}
