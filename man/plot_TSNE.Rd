% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proteome_wide_diagnostics.R
\name{plot_TSNE}
\alias{plot_TSNE}
\alias{plot_TSNE.default}
\alias{plot_TSNE.ProBatchFeatures}
\title{Plot a t-SNE embedding of samples}
\usage{
\method{plot_TSNE}{default}(
  data_matrix,
  sample_annotation,
  feature_id_col = "peptide_group_label",
  sample_id_col = "FullRunName",
  color_by = "MS_batch",
  shape_by = NULL,
  tsne_dims = 2,
  perplexity = 30,
  initial_dims = 50,
  max_iter = 1000,
  fill_the_missing = -1,
  color_scheme = "brewer",
  plot_title = NULL,
  point_size = 3,
  point_alpha = 0.85,
  random_seed = NULL,
  use_plotlyrender = FALSE,
  theme_name = "classic",
  base_size = 10,
  plotly_param = list(width = 800, height = 600),
  ...
)

\method{plot_TSNE}{ProBatchFeatures}(
  data_matrix,
  pbf_name = NULL,
  sample_annotation = NULL,
  sample_id_col = "FullRunName",
  plot_title = NULL,
  use_plotlyrender = FALSE,
  assay_args = NULL,
  return_gridExtra = FALSE,
  plot_ncol = NULL,
  return_subplots = FALSE,
  subplot_ncol = NULL,
  share_axes = TRUE,
  ...
)

plot_TSNE(data_matrix, ...)
}
\arguments{
\item{data_matrix}{Numeric feature-by-sample matrix or a
\code{ProBatchFeatures} object.}

\item{sample_annotation}{data frame with:
\enumerate{ \item \code{sample_id_col} (this can be repeated as row names)
  \item biological covariates
  \item technical covariates (batches etc) }.
  See \code{help("example_sample_annotation")}}

\item{feature_id_col}{name of the column with feature/gene/peptide/protein
ID used in the long format representation \code{df_long}. In the wide
formatted representation \code{data_matrix} this corresponds to the row
names.}

\item{sample_id_col}{name of the column in \code{sample_annotation} table,
where the filenames (colnames of the \code{data_matrix} are found).}

\item{color_by}{Column name in \code{sample_annotation} to color by.
Factor-like and numeric columns are supported.}

\item{shape_by}{Optional factor-like annotation column used for point
shapes.}

\item{tsne_dims}{Integer number of embedding dimensions, at least two. The
first two dimensions are plotted.}

\item{perplexity}{Positive t-SNE perplexity. Values above
\code{max(1, floor((n_samples - 1) / 3))} are reduced with a warning.}

\item{initial_dims}{Positive integer number of input dimensions retained by
the backend's initial PCA step.}

\item{max_iter}{Positive integer number of t-SNE iterations.}

\item{fill_the_missing}{Missing-value handling shared by PCA, t-SNE, and
UMAP. A finite numeric scalar replaces missing values and defaults to
\code{-1}; \code{NULL} or \code{FALSE} removes incomplete features. The
function warns before applying either non-error handling path.}

\item{color_scheme}{a named vector of colors to map to \code{batch_col},
names corresponding to the levels of the factor. For continuous variables,
vector doesn't need to be named.}

\item{plot_title}{title of the plot (e.g., processing step + representation
level (fragments, transitions, proteins) + purpose (meanplot/corrplot etc))}

\item{point_size}{Point size supplied to \code{ggplot2::geom_point()}.}

\item{point_alpha}{Alpha transparency for plotted points.}

\item{random_seed}{Optional integer seed for reproducible t-SNE coordinates.
When \code{NULL}, the caller controls the R random-number state.}

\item{use_plotlyrender}{Logical; return a \code{plotly} object instead of a
\code{ggplot}.}

\item{theme_name}{Plot theme name. Only \code{"classic"} is currently
implemented; use \code{NULL} for the default ggplot theme.}

\item{base_size}{base size of the text in the plot}

\item{plotly_param}{Named list of optional arguments such as \code{width} and
\code{height} passed to \code{plotly::plot_ly()}.}

\item{...}{Named backend arguments forwarded to \code{Rtsne::Rtsne()} by the
default method. In the \code{ProBatchFeatures} method these are shared
default-method arguments.}

\item{pbf_name}{Assay name(s) used when \code{data_matrix} is a
\code{ProBatchFeatures}.}

\item{assay_args}{\code{ProBatchFeatures} method only. A named list keyed by
selected assay, with each value a named list that overrides shared
default-method arguments for that assay.}

\item{return_gridExtra}{Logical; for multiple assays, return an invisible
list containing the arranged \code{grob} and named ordered \code{plots}.}

\item{plot_ncol}{Number of columns when arranging multiple assay plots.}

\item{return_subplots}{Logical; combine multiple interactive assay plots with
\code{plotly::subplot()}.}

\item{subplot_ncol}{Optional positive integer number of subplot columns.}

\item{share_axes}{One logical value shared by both axes, or two logical
values for the x and y axes.}
}
\value{
For matrix input, a \code{ggplot} or, when requested, a \code{plotly} object.
Plot data follow the sample order in the input matrix columns.
For one selected \code{ProBatchFeatures} assay, the corresponding single plot.
For multiple assays, an arranged static result, a named ordered list of
interactive plots, or an explicitly requested Plotly subplot.
}
\description{
Compute t-SNE with samples as observations (matrix columns) and return a
static \code{ggplot} by default. Interactive Plotly rendering is optional.
}
\details{
Each input assay must contain at least two samples.
}
\examples{
sample_ids <- paste0("sample_", seq_len(8))
feature_ids <- paste0("feature_", seq_len(12))
embedding_matrix <- matrix(
    seq_len(96) + sin(seq_len(96)),
    nrow = 12,
    dimnames = list(feature_ids, sample_ids)
)
embedding_annotation <- data.frame(
    FullRunName = sample_ids,
    MS_batch = rep(c("batch_1", "batch_2"), each = 4)
)
\donttest{
if (requireNamespace("Rtsne", quietly = TRUE)) {
    tsne_plot <- plot_TSNE(
        embedding_matrix,
        embedding_annotation,
        perplexity = 2,
        max_iter = 250,
        random_seed = 7
    )
}
}
}
