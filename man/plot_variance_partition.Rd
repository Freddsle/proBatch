% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/explained_variance_plots.R
\name{plot_variance_partition}
\alias{plot_variance_partition}
\alias{plot_variance_partition.default}
\alias{plot_variance_partition.ProBatchFeatures}
\title{Plot variance partition distributions}
\usage{
\method{plot_variance_partition}{default}(
  data_matrix,
  sample_annotation,
  feature_id_col = "peptide_group_label",
  sample_id_col = "FullRunName",
  technical_factors = c("MS_batch", "instrument"),
  biological_factors = c("cell_line", "drug_dose"),
  fill_the_missing = -1,
  model_formula = NULL,
  model_variables = NULL,
  variance_threshold = NULL,
  abundance_top_n = NULL,
  abundance_direction = c("most", "least"),
  abundance_stat = c("mean", "median"),
  colors_for_boxes = NULL,
  filename = NULL,
  width = NA,
  height = NA,
  units = c("cm", "in", "mm"),
  plot_title = NULL,
  theme = "classic",
  base_size = 15,
  add_medians = FALSE,
  median_position = 0.05,
  summary_stat = c("boxplot", "mean", "median"),
  show_legend = TRUE,
  y_limits = c(0, 1),
  path_to_save_results = NULL,
  ...
)

\method{plot_variance_partition}{ProBatchFeatures}(
  data_matrix,
  pbf_name = NULL,
  sample_annotation = NULL,
  feature_id_col = "peptide_group_label",
  sample_id_col = "FullRunName",
  colors_for_boxes = NULL,
  filename = NULL,
  width = NA,
  height = NA,
  units = c("cm", "in", "mm"),
  plot_title = NULL,
  theme = "classic",
  base_size = 20,
  return_gridExtra = FALSE,
  plot_ncol = NULL,
  organize_pbfs = FALSE,
  path_to_save_results = NULL,
  ...
)
}
\arguments{
\item{data_matrix}{features (in rows) vs samples (in columns) matrix, with
feature IDs in rownames and file/sample names as colnames.
See "example_proteome_matrix" for more details (to call the description,
use \code{help("example_proteome_matrix")})}

\item{sample_annotation}{data frame with:
\enumerate{ \item \code{sample_id_col} (this can be repeated as row names)
  \item biological covariates
  \item technical covariates (batches etc) }.
  See \code{help("example_sample_annotation")}}

\item{feature_id_col}{name of the column with feature/gene/peptide/protein
ID used in the long format representation \code{df_long}. In the wide
formatted representation \code{data_matrix} this corresponds to the row
names.}

\item{sample_id_col}{name of the column in \code{sample_annotation} table,
where the filenames (colnames of the \code{data_matrix} are found).}

\item{technical_factors}{vector with technically driven covariates.}

\item{biological_factors}{vector with biologically driven covariates.}

\item{fill_the_missing}{numeric value determining how missing values should be
substituted. If \code{NULL}, features with missing values are excluded.}

\item{model_formula}{model formula passed to
`variancePartition::fitExtractVarPartModel()`.}

\item{model_variables}{vector of \code{sample_annotation} column names used to
build a formula when `model_formula` is `NULL`.}

\item{variance_threshold}{numeric threshold; values below the threshold are
summed per feature and reported as a single group.}

\item{abundance_top_n}{optional integer specifying how many of the most/least
abundant features to keep for plotting.}

\item{abundance_direction}{character, either `"most"` or `"least"`, defining
which tail of the abundance ranking to keep.}

\item{abundance_stat}{character, either `"mean"` or `"median"`, indicating
how abundance is computed across samples before ranking (after missing-value
handling).}

\item{colors_for_boxes}{four-item color vector, specifying colors for the
following categories: c('residual', 'biological', 'biol:techn', 'technical').}

\item{filename}{path where the results are saved.
If null the object is returned to the active window;
otherwise, the object is save into the file. Currently only
pdf and png format is supported}

\item{width}{option  determining the output image width}

\item{height}{option  determining the output image height}

\item{units}{units: 'cm', 'in' or 'mm'}

\item{plot_title}{optional plot title.}

\item{theme}{ggplot2 theme to apply. Currently, only `"classic"` is supported.}

\item{base_size}{base size of the text in the plot.}

\item{add_medians}{logical; when `TRUE`, annotates each box with its rounded
median value (boxplot mode only).}

\item{median_position}{numeric adjustment applied to the median labels when
they are nudged upwards.}

\item{summary_stat}{character defining the plot summary to draw: `"boxplot"`
(default), `"mean"`, or `"median"`. When `"mean"` or `"median"`, the plot
shows a single summary value per label instead of a distribution.}

\item{show_legend}{logical; controls legend display.}

\item{y_limits}{numeric vector of length two specifying y-axis limits.}

\item{path_to_save_results}{optional path to save the prepared results as CSV.}

\item{...}{Additional arguments forwarded to `prepare_variance_partition_df()`
(and further to `variancePartition::fitExtractVarPartModel()`).}

\item{return_gridExtra}{logical; when `TRUE`, returns arranged grobs instead of
a list of plots (for multi-assay objects).}

\item{plot_ncol}{number of columns when arranging multiple assay plots.}

\item{organize_pbfs}{logical; when `TRUE` (multi-assay inputs only), derives and
applies shared y-axis limits across all subplots.}
}
\value{
ggplot object (or list of ggplot objects for multi-assay inputs).
}
\description{
Plot variance partition distributions
}
\examples{
if (requireNamespace("variancePartition", quietly = TRUE)) {
    data(list = c("example_proteome_matrix", "example_sample_annotation"), package = "proBatch")
    matrix_test <- na.omit(example_proteome_matrix)[1:30, ]
    plot_variance_partition(
        matrix_test,
        example_sample_annotation,
        technical_factors = "MS_batch",
        biological_factors = c("Diet", "Sex"),
        variance_threshold = 0.05,
        abundance_top_n = 20,
        abundance_direction = "most",
        summary_stat = "median"
    )
}
}
