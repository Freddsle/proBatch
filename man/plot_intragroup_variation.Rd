% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/calculate_intragroup.R
\name{plot_intragroup_variation}
\alias{plot_intragroup_variation}
\alias{plot_intragroup_variation.default}
\alias{plot_intragroup_variation.ProBatchFeatures}
\title{Plot intragroup variation diagnostics}
\usage{
\method{plot_intragroup_variation}{default}(
  data_matrix,
  sample_annotation,
  group_col,
  feature_id_col = "peptide_group_label",
  sample_id_col = "FullRunName",
  fill_the_missing = FALSE,
  metrics = "correlation",
  correlation_method = c("pearson", "spearman", "kendall"),
  pcv_diff = TRUE,
  pmad_diff = FALSE,
  pev_diff = FALSE,
  assay_label = NULL,
  metric_plot_ncol = 2,
  filename = NULL,
  width = NA,
  height = NA,
  units = c("cm", "in", "mm"),
  path_to_save_results = NULL,
  plot_title = NULL,
  base_size = 12
)

\method{plot_intragroup_variation}{ProBatchFeatures}(
  data_matrix,
  pbf_name = NULL,
  sample_annotation = NULL,
  group_col,
  feature_id_col = "peptide_group_label",
  sample_id_col = "FullRunName",
  fill_the_missing = FALSE,
  plot_title = NULL,
  return_gridExtra = FALSE,
  plot_ncol = NULL,
  path_to_save_results = NULL,
  metric_plot_ncol = 2,
  ...
)
}
\arguments{
\item{data_matrix}{Numeric matrix (features × samples) when calling the
`default` method.}

\item{sample_annotation}{data frame with:
\enumerate{ \item \code{sample_id_col} (this can be repeated as row names)
  \item biological covariates
  \item technical covariates (batches etc) }.
  See \code{help("example_sample_annotation")}}

\item{group_col}{Sample annotation column(s) that define intragroup
membership. Provide a single column name or a character vector; every
requested column must exist in `sample_annotation` and contain no missing
values.}

\item{feature_id_col}{name of the column with feature/gene/peptide/protein
ID used in the long format representation \code{df_long}. In the wide
formatted representation \code{data_matrix} this corresponds to the row
names.}

\item{sample_id_col}{name of the column in \code{sample_annotation} table,
where the filenames (colnames of the \code{data_matrix} are found).}

\item{fill_the_missing}{Missing-value policy applied to `data_matrix` before
calling PRONE. Defaults to `FALSE` (keep missing values). See
[handle_missing_values()] for the supported options.}

\item{metrics}{Character vector choosing one or more of
`c("correlation", "PCV", "PMAD", "PEV")`.}

\item{correlation_method}{Correlation method passed to
[PRONE::plot_intragroup_correlation()].}

\item{pcv_diff, pmad_diff, pev_diff}{Logical scalars forwarded to the
corresponding PRONE plotting routines to request `diff = TRUE`.}

\item{assay_label}{Optional label used when constructing the temporary
`SummarizedExperiment` that is passed to PRONE. When `NULL`, the function
reuses the assay name (for `ProBatchFeatures`) or a generic label.}

\item{metric_plot_ncol}{Number of columns used when arranging the per-metric
plots for a single assay.}

\item{filename}{Optional output path for the combined plot of all metrics
(single assay).}

\item{width}{option  determining the output image width}

\item{height}{option  determining the output image height}

\item{units}{units: 'cm', 'in' or 'mm'}

\item{path_to_save_results}{Optional directory where the per-metric data are
written as CSV files. When operating on a `ProBatchFeatures` instance, set
this to a parent directory; one sub-directory per assay is created.}

\item{plot_title}{title of the plot (e.g., processing step + representation
level (fragments, transitions, proteins) + purpose (meanplot/corrplot etc))}

\item{base_size}{Base font size used when annotating metric titles.}

\item{pbf_name}{Names of assays to process when `data_matrix` is a
`ProBatchFeatures` object (see [pb_assay_matrix()]).}

\item{return_gridExtra}{Logical; request the `gridExtra` object instead of a
ggplot-ready object for multi-assay layouts.}

\item{plot_ncol}{Number of columns when arranging multiple assays (only for
the `ProBatchFeatures` method).}

\item{...}{Additional arguments forwarded to the `default` method (for
example `metrics`, `correlation_method`, `pcv_diff`, and sizing arguments).}
}
\value{
A `ggplot` object showing the requested intragroup metric(s). The
  object carries two attributes: `pb_intragroup_metrics` (a list of exported
  metric tables) and `pb_intragroup_saved_files` (paths to saved CSV files
  when `path_to_save_results` is provided).
}
\description{
proBatch provides wrappers around the intragroup evaluation plots implemented
in the [PRONE](https://github.com/daisybio/PRONE) package so that they can be
applied directly to matrices or `ProBatchFeatures` pipelines. The PRONE
functions are used as-is for the individual metrics, while proBatch aligns the
supplied sample annotation, collects the per-metric data, arranges the plots,
and optionally stores the tabular results.
}
