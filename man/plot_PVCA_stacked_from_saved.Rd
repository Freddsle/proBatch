% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/proteome_wide_diagnostics.R
\name{plot_PVCA_stacked_from_saved}
\alias{plot_PVCA_stacked_from_saved}
\title{Plot stacked PVCA results from saved CSV files}
\usage{
plot_PVCA_stacked_from_saved(
  pvca_dir,
  sort_stacked = NULL,
  colors_for_bars = NULL,
  plot_title = NULL,
  stacked_plot_title = "Plot of weighted average proportion variance vs effects in PVCA",
  theme = "classic",
  base_size = 15,
  filename = NULL,
  width = NA,
  height = NA,
  units = c("cm", "in", "mm"),
  category_order = NULL
)
}
\arguments{
\item{pvca_dir}{Directory that contains per-assay
\code{PVCA_results_aggregated.csv} files (the function searches recursively).}

\item{sort_stacked}{Optional factor label used to order the rows of the
stacked plot (matching \code{sort_stacked} in \link{plot_PVCA.ProBatchFeatures}).}

\item{colors_for_bars}{Character vector of four colors (residual, biological,
biol:techn, technical) passed to the stacked bar helper.}

\item{plot_title}{Fallback plot title used when
\code{stacked_plot_title = NULL}.}

\item{stacked_plot_title}{Optional character vector used as the stacked plot
title; elements are joined with newline characters. Its non-\code{NULL} default
takes precedence over \code{plot_title}.}

\item{theme}{Plot theme; only \code{"classic"} is currently implemented.}

\item{base_size}{Base font size passed to \code{theme_classic()}.}

\item{filename}{Optional path to save the stacked plot.}

\item{width}{Plot width forwarded to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{height}{Plot height forwarded to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{units}{Plot units forwarded to \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}.}

\item{category_order}{Optional character vector specifying the order of
categories in the stacked plot.}
}
\value{
A \code{ggplot} object showing the stacked PVCA weights.
}
\description{
When PVCA analysis has been run previously and the aggregated weights were
written to disk (one \code{PVCA_results_aggregated.csv} per assay), this helper
rebuilds the stacked bar summary without rerunning the analysis.
}
\examples{
pvca_dir <- tempfile("saved_pvca_")
pvca_plot <- tryCatch({
    assay_dir <- file.path(pvca_dir, "assay_1")
    dir.create(assay_dir, recursive = TRUE)
    saved_pvca <- data.frame(
        label = c("Diet", "resid"),
        weights = c(0.7, 0.3),
        category = c("biological", "residual")
    )
    utils::write.csv(
        saved_pvca,
        file.path(assay_dir, "PVCA_results_aggregated.csv"),
        row.names = FALSE
    )
    plot_PVCA_stacked_from_saved(pvca_dir)
}, finally = {
    unlink(pvca_dir, recursive = TRUE)
})
}
